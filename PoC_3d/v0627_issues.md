1) double check the loss curve plotting labels (eg, make sure epochs and steps labels are correct)

2) make sampler able to make more reference samples from raw data using nearest neighbot logic on condition vector

3) when training with a coarse location condition, the plotted load vectors dont show full arrow- just arrowhead.

4) check label logic in plots- is it the labels we want to see? what would be more informative?

5) make better notion of "best" for checkpoint saving

