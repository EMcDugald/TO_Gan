1) n-spatial-bins is not technically used in "fully fine" mode, but current implementation of train data generators still expects it under that configuration, even though it isnt used.

2) improve visualizations of coarse samples


